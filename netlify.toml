[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

# Extend timeout for generate function — AI report generation needs up to 26s
[functions."generate"]
  timeout = 26

[functions."capture"]
  timeout = 10
